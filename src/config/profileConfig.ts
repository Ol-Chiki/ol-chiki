
import type { ProfileConfigType } from '@/types/profile';

export const profileConfig: ProfileConfigType = {
  maxDisplayNameLength: 50,
  allowedImageTypes: ['image/jpeg', 'image/png', 'image/gif'],
  maxImageSizeMB: 2,
};
